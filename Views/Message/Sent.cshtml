@model List<WebProject.Models.Message>

    <h2>Gönderilen Mesajlar</h2>

    @foreach (var msg in Model)
{
    <div class="border p-2 mb-2">
        <strong>@msg.Receiver.UserName</strong> - <small>@msg.SentAt.ToString("g")</small>
        <p>@msg.Content</p>
    </div>
}
